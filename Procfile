# ==========================================
# 🦍 Gorilla-Link Procfile
# ==========================================
# This ensures consistent startup for all environments

web: gunicorn app_pro:app --timeout 120 --workers 4 --threads 2 --log-level info
