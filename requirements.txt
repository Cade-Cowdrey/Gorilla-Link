# =============================================================
# PittState-Connect — Requirements (Advanced / Render Edition)
# =============================================================

# --- Core Flask Framework ---
Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.4
itsdangerous==2.2.0

# --- Database + ORM ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.32
Flask-Migrate==4.0.7
psycopg2-binary==2.9.9   # ✅ PostgreSQL driver (prebuilt for Render)

# --- Auth / Sessions ---
Flask-Login==0.6.3
Flask-WTF==1.2.1
email-validator==2.2.0

# --- Mail / Notifications ---
Flask-Mail==0.9.1

# --- API + Security Enhancements ---
Flask-Cors==4.0.1
Flask-Compress==1.14
gunicorn==22.0.0          # ✅ Production server for Render
Werkzeug-ProxyFix==1.0.0  # optional but improves proxy handling

# --- Utility / Background / Scheduling ---
python-dotenv==1.0.1
requests==2.32.3
schedule==1.2.1
APScheduler==3.10.4
redis==5.0.3               # (optional future async worker support)

# --- AI / OpenAI Integrations ---
openai==1.43.0             # ✅ Smart Match, Essay Scorer, Mentor AI helper

# --- Email + Template Rendering Helpers ---
markdown2==2.5.0
bleach==6.1.0              # sanitize AI-generated HTML safely

# --- Analytics / Data Visualization (optional future dashboards) ---
pandas==2.2.3
matplotlib==3.9.2
plotly==5.24.1

# --- Monitoring / Error Reporting ---
sentry-sdk==2.16.0

# --- Testing / Diagnostics ---
pytest==8.3.3
pytest-cov==5.0.0

# --- Development Tools (safe to leave for prod) ---
black==24.10.0
flake8==7.1.0
isort==5.13.2
